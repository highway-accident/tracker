{% extends "FOSUserBundle::layout.html.twig" %}

{% block fos_user_content %}
<div class="block block_type_login">
    <div class="block__head">
        Вход
    </div>

    <div class="block__content">
        {% if error %}
        <div class="block__content__errors">{{ error }}</div>
        {% endif %}
        <form class="fos_user_security_login"action="{{ path("fos_user_security_check") }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />

            <table class="form_holder">
                <tr>
                    <td><label for="username">Логин:</label></td>
                    <td><input type="text" id="username" name="_username" value="{{ last_username }}" /></td>
                </tr>

                <tr>
                    <td><label for="password">Пароль:</label></td>
                    <td><input type="password" id="password" name="_password" /></td>
                </tr>
            </table>
                <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                <label for="remember_me">Запомнить меня</label><br />

            <a href="{{ path('fos_user_resetting_request') }}">Забыли пароль?</a>
            <input type="submit" id="_submit" name="_submit" value="Войти" />
        </form>
    </div>
</div>
{% endblock fos_user_content %}